%h2 Global goals

%table.table.table-striped.table-bordered
  %tr
    %th Title
    %th Description
    %th
    %th
    %th

  - @goals.each do |goal|
    %tr
      %td= goal.title
      %td= truncate(strip_tags(goal.description), length: 115)
      %td= link_to 'Show', goal, class: "btn btn-default btn-sm"
      %td= link_to 'Edit', edit_goal_path(goal), class: "btn btn-default btn-sm"
      %td= link_to 'Delete', goal, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm"

%br

= link_to 'Add Goal', new_goal_path, class: "btn btn-success btn-sm"